ActiveAdmin.register Donation do

  # See permitted parameters documentation:
  # https://github.com/activeadmin/activeadmin/blob/master/docs/2-resource-customization.md#setting-up-strong-parameters
  #
  # Uncomment all parameters which should be permitted for assignment
  #
  # permit_params :amount, :currency, :frequency, :full_name, :subscribe_newsletter, :include_name, :stripe_session_id, :stripe_customer_id, :stripe_payment_id, :stripe_subscription_id, :status, :donation_type, :campaign_id, :session_id
  #
  # or
  #
  # permit_params do
  #   permitted = [:amount, :currency, :frequency, :full_name, :subscribe_newsletter, :include_name, :stripe_session_id, :stripe_customer_id, :stripe_payment_id, :stripe_subscription_id, :status, :donation_type, :campaign_id, :session_id]
  #   permitted << :other if params[:action] == 'create' && current_user.admin?
  #   permitted
  # end
  
end
